# ── Backend ───────────────────────────────────────────────────────────────────
fastapi==0.111.0          # async REST framework
uvicorn[standard]==0.29.0 # ASGI server to run FastAPI
pydantic==2.7.1           # data validation (FastAPI uses this internally)

# CodeBERT — HuggingFace Transformers + PyTorch
transformers==4.41.1      # RobertaTokenizer + RobertaModel
torch==2.3.0              # deep learning backend (CPU build is fine for inference)

# Groq LLM API client
groq==0.9.0               # official Groq Python SDK

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit==1.35.0         # UI framework
requests==2.32.2          # HTTP client used by the frontend to call FastAPI
